# 更新日志

## v1.9.0

- 性能：构建产物改用 esbuild 压缩（content.js 815KB→494KB，体积 -40%），构建脚本 scripts/rebuild-bundles.js 支持 --check 幂等校验与 --no-minify 调试；layout-style.js 中的 CSS 模板字符串同步压缩
- 性能：全局 MutationObserver 局部化——导航 observer 排除扩展自插 DOM 并加防抖、关键词高亮只扫新增子树、评论草稿 observer 收窄范围；多处 scroll/resize 监听加 rAF 节流；内存缓存（评论/表情/等级/AI 总结）改用 LRU 上限淘汰
- 新增：「AI 模型」设置页——自定义服务商管理，支持自定义供应商、拉取模型列表、测试连通（内置 Gemini 本地模型因占用内存过大已移除）
- 新增：长帖 AI 导读——超长帖子标题旁一键生成「结论+3 要点」
- 新增：促销帖省钱助手——自动解析限免/折扣帖为卡片，并记录史低价格对比
- 新增：AI 语义屏蔽——用 AI 识别剧透/引战/软广并自动折叠，需在「屏蔽」页手动开启，支持意图自定义
- 新增：我的足迹——自动记录浏览过的帖子（「我读过的」），设置面板「足迹」页聚合浏览历史与稍后读，支持搜索/删除/清空
- 新增：AI 建议回复显示生成耗时（回帖「✨ AI」面板底部）
- 修复：信息流评论预览栏时有时无——Observer 排除逻辑把移入扩展容器的帖子误判为扩展自有节点，导致懒加载新帖子不触发增强；改为按帖子特征触发
- 修复：评论区观点总结与版本弹窗 appendChild 报错（HTML 字符串误用 appendChild，改为 innerHTML）
- 修复：设置面板版本号与实际版本不同步（EXTENSION_VERSION 未随发布更新），点击版本号现展示 v1.9.0 真实更新内容

## v1.8.0

- 移除：评论预览「只看楼主」开关与详情页「只看楼主」按钮，修复「看楼主」文字与开关圆点重叠问题
- 恢复：评论预览工具栏三段式排序（默认 / 最新 / 作者优先）、📊 观点总结与屏蔽 CY 开关
- 优化：设置面板点击版本号直接显示本版本更新内容，不再依赖 GitHub 在线拉取（离线可用）
- 保留：主评论楼层号 #N 显示

## v1.7

- 新增：AI 建议回复——评论预览回复表单新增「✨ AI」按钮，AI 基于帖子内容与当前回复目标评论生成 3 条候选回复（认真客观 / 轻松幽默 / 简短直接三种风格标签），点击候选直接发送，无需二次确认；支持「🔄 重新生成」
- 新增：评论区观点总结——评论预览工具栏新增「📊 观点总结」按钮，AI 总结评论区主要观点（按支持度排序并标注倾向）、争议点、高赞评论代表观点与整体风向；按帖子缓存，支持重新生成
- 新增：评论预览只看楼主开关（与屏蔽 CY 并排，状态持久化）与主评论楼层号显示（#N，按原始楼层，排序切换不变）
- 新增：设置面板点击版本号可查看当前版本更新内容——在线优先拉取 GitHub CHANGELOG.md 当前版本条目，离线/失败时回退内置文案；支持点击外部 / ESC / 关闭按钮关闭

## v1.6

- 新增：设置面板顶部版本号现在会检测 GitHub 发布的正式版，发现新版本时显示「发现新版本 vX.Y」提示，点击可打开 GitHub Releases 查看更新内容；检测结果缓存 24 小时，请求失败静默不打扰
- 新增：运行日志支持按状态筛选（全部 / 成功 / 错误 / 提醒 / 信息），筛选状态持久化，切换视图时筛选条自动隐藏
- 新增：运行日志改为分页展示（每页 200 条），5000 条日志也可流畅浏览，详情展开 / 复制恢复正常
- 优化：「刷新日志」「清空日志」按钮上移到标题行，操作更顺手
- 优化：全局滚动监听加 rAF 节流，信息流滚动时不再每帧强制布局；搜索历史输入加防抖

## v1.5

- 修复：运行日志页“全部/@消息/评论/回复/首页推荐帖”筛选按钮在运行日志视图下错误显示（display:flex 覆盖 [hidden]），点击后看不到过滤效果；现已只在消息日志视图显示并正常过滤
- 开发：CRX 打包私钥 key.pem 不再存放于项目根目录（避免 Chrome“加载已解压扩展”时提示项目内含密钥文件），自动迁移至 ~/.better-xiaoheihe/，可用 BETTER_XIAOHEIHE_KEY_PEM 指定路径

- AI 统计 / 消息日志条数上限从 500 提升到 5000，统计数据不再封顶在 500 条回复
- AI 统计 tab 进入时每 10 秒自动刷新统计卡片与回复记录
- 修复：AI 统计页“最近 7 天回复分布”仍可能重复渲染——刷新时整段插入两块内容但只替换了统计卡片节点，旧周分布图残留累积；现改为整体容器 1:1 替换并清理游离图表
- 运行日志新增“当前执行中”记录：处理 @ / 评论消息、评论首页推荐帖时直接显示正在执行的帖子标题
- 修复：消息弹窗“等 N 人”点赞数解析字段不全（补齐 digg / award / praise / like 等字段），分页加载提前结束的问题（识别接口 no_more）
- 设置面板账号栏新增“获赞总数”显示（从小黑盒账号接口解析，取不到则不显示）

## v1.4

- 运行日志恢复为独立 tab：进入自动刷新（每 10 秒），运行日志 / 消息日志 / 待处理消息切换，AI Bot 页移除“查看运行日志”按钮
- 修复：AI 统计页“最近 7 天回复分布”因刷新替换结构不一致而累积重复显示
- 回复记录增加分页（每页 10 条，上一页 / 下一页）
- 修复：设置 tab 栏被 flex-wrap 规则覆盖，不再平摊顶部空间
- 修复：@ 消息通知开关缺少开启态样式，点击后滑块无视觉反馈（表现为打不开）
- 通用设置：外观（明暗主题）从“更多设置”折叠区挪至信息流布局下方直显
- 稍后读：去掉导出 Markdown，直接显示收藏的帖子列表

## v1.3

- 首页推荐帖评论轮询最小间隔从 10 分钟降至 3 分钟
- README 去除 fork 标记，定位为独立项目

## v1.2

- AI 统计独立 tab：统计卡片 + 7 天分布 + 回复构成 + AI 总结历史 + 回复记录，支持弹窗提问 / 打开帖子提问
- UI 优化：账号栏精简为头像 + 名称；通用设置次要功能折叠为更多设置；tab 间距优化
- 修复：content 脚本运行在 MAIN world（无扩展 API），storage 全部改走 ai-bridge 事件桥接
- 样式：补信息流稍后读按钮样式
- 暖贴智能挑帖 + 收藏导出 Markdown
- 关键词高亮 + 评论草稿箱 + 稍后读（导出 Markdown）+ 详情页只看楼主
- AI Bot 风控保护层 + 人设预设 + 回复仪表盘；@ 消息浏览器通知
- 新增设置面板账号信息栏（头像/昵称/等级+经验进度/勋章）+ 明暗主题（跟随系统/亮/暗）
- 隐藏设置面板推广位卡片；开源项目卡片指向 t479842598/Better-Black-Box
- 恢复 AI Bot 自动回复功能（基于商店版 1.2）

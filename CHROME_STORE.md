# Chrome Web Store 上架资料

## 基本信息

名称：

```text
更好的小黑盒
```

简短说明：

```text
优化小黑盒社区首页，在帖子右侧直接预览热门评论。
```

详细说明：

```text
🔥 更好的小黑盒

让信息流不仅能看帖子，也能直接看讨论。

这是一款面向小黑盒网页社区的轻量增强插件。它会在帖子列表右侧增加评论预览区，让你不用频繁进入详情页，也能快速判断一条内容是否值得继续看。


【评论预览】

在首页、话题链接页、个人主页、收藏页和搜索页的信息流中，每条帖子右侧都会显示评论预览。

你可以直接看到：

  用户头像、昵称、等级
  评论正文、图片、游戏链接
  发布时间、IP 属地
  点赞数、作者标识
  楼中楼回复

看到有价值的讨论，再进入详情页；没营养的内容，直接略过。


【高效浏览】

评论区高度会跟随左侧帖子内容自动适配。

评论很多时，只在右侧评论区内部滚动，不会把整条帖子撑得很长，也不会打乱信息流节奏。

你可以在列表页直接：

  滚动加载更多评论
  点击“全部 N 条回复”展开楼中楼
  给主评论和楼中楼回复点赞
  点击用户信息跳转到主页
  点击评论图片查看大图
  在帖子底部直接点赞内容


【页面布局优化】

插件会把左侧菜单折叠到顶部栏，并隐藏原右侧推荐栏，释放更多横向空间。

顶部栏还提供：

  收藏入口
  设置入口
  左侧菜单入口

这些入口保持轻量，不改变小黑盒原有使用习惯。


【评论屏蔽】

支持屏蔽插眼评论。

开启后，开关右侧会显示本次被屏蔽的 CY 评论数量。

也支持添加评论屏蔽关键词。命中关键词的评论会直接隐藏，每个关键词的屏蔽生效次数会保存在浏览器本地。

被屏蔽的楼中楼回复不会继续显示在“全部 N 条回复”的数量中，避免出现回复数和实际展示不一致。


【AI Bot】

用户可在设置页独立配置 AI 服务商、Base URL、模型、API Key、轮询评论和 @ 周期、只处理最近消息时间窗口、回复 @ 开关、回复评论开关、评论首页推荐帖开关、白名单用户 ID、评论提示词、是否允许表情，以及跨帖子历史对话开关和数量。

开启“回复 @”后，浏览器运行期间会自动查询当前登录账号的 @ 消息；符合白名单规则时，会读取帖子详情和评论区上下文，调用用户配置的 AI 接口生成回复，并使用当前小黑盒网页登录态回复到触发 @ 的评论下。

“回复 @”“回复评论”和“评论首页推荐帖”默认关闭；首次进入 AI Bot 设置时会单独展示风险说明，并要求用户勾选后明确确认授权。开启后会记录本地运行日志和已回复记录，避免重复回复。跨帖子历史对话默认开启，会按当前账号和相关用户 ID 使用最近 7 天内最多 20 组成功互动，可关闭或调整为 1-100 组；成功首页暖贴会按帖子作者 ID 记录为我方主动评论。自动评论连续发送失败 3 次后，插件会关闭 AI 回复和首页暖贴，并通知用户检查账号状态。


【适合谁】

  高频浏览小黑盒社区的用户
  想快速判断帖子讨论质量的用户
  更关注评论区观点和氛围的玩家
  不想反复点进详情页再退回列表的用户


【隐私说明】

插件不设置开发者数据服务器，不向插件开发者上传或出售用户数据，也不将数据用于广告或追踪。

评论数据来自小黑盒网页自身接口，只用于当前页面展示。

屏蔽 CY 状态、屏蔽关键词、等级过滤规则和关键词生效次数只保存在当前浏览器的扩展本地存储中，不会发送到小黑盒或其他服务器。

用户主动使用 AI 总结或开启 AI Bot 后，帖子标题、正文、图片链接、评论文本、相关昵称或用户 ID 和所需上下文会发送到用户自行配置的第三方 AI 服务商。API Key 保存在扩展本地存储中，并在小黑盒网页内的插件设置面板中编辑。
```

分类建议：

```text
Productivity
```

语言：

```text
中文（简体）
```

## 权限说明

本插件申请 `storage` 权限用于在用户浏览器本地保存插件设置，包括 AI 总结设置、AI Bot 设置与日志、屏蔽关键词、等级过滤规则和相关本地偏好；这些设置不会上传到插件作者服务器。插件申请 `alarms` 权限用于 AI Bot 任一回复开关开启后按用户设置周期轮询对应消息。插件申请 `notifications` 权限用于 AI Bot 检测到小黑盒登录状态过期时提醒用户。插件申请 `cookies` 权限用于 AI Bot 在后台读取当前小黑盒登录 Cookie 中的用户 ID 并让请求使用现有登录态；插件不会保存、上传、删除或修改这些 Cookie。插件申请 `declarativeNetRequestWithHostAccess` 权限，仅用于右侧评论区读取接口发送前替换请求头 Cookie，去除 `heybox_id` 和 `user_heybox_id`。插件申请主机权限以支持用户自定义 AI Base URL 的模型拉取、连通测试、总结请求和 AI Bot 回复生成请求。

页面匹配范围：

```text
https://www.xiaoheihe.cn/app/bbs
https://www.xiaoheihe.cn/app/bbs/*
https://www.xiaoheihe.cn/app/topic/link
https://www.xiaoheihe.cn/app/topic/link/*
https://www.xiaoheihe.cn/app/user/profile
https://www.xiaoheihe.cn/app/user/profile/*
https://www.xiaoheihe.cn/app/user/favour
https://www.xiaoheihe.cn/app/user/favour/*
https://www.xiaoheihe.cn/app/search
https://www.xiaoheihe.cn/app/search*
```

用途说明：

```text
增强小黑盒网页社区的信息流浏览、评论预览、内容过滤，以及由用户主动配置和开启的 AI 总结与辅助互动。
```

主机权限使用理由：

```text
插件需要访问 https://www.xiaoheihe.cn/app/bbs 及其子路径，以便只在小黑盒社区页面运行内容脚本。内容脚本用于调整页面布局、折叠导航菜单、隐藏右侧推荐栏，并在信息流帖子右侧展示评论预览和评论/AI 总结表情图片映射。插件还需要访问用户在设置弹框中填写的 AI Base URL，用于用户主动配置后的模型拉取、连通测试、帖子总结和 AI Bot 自动回复生成请求。
```

storage 权限使用理由：

```text
用于在用户浏览器的扩展本地存储中保存插件设置，包括 AI 总结设置、AI 总结是否允许表情、AI Bot 回复开关、AI Bot 服务商类型、AI Bot Base URL、模型、API Key、轮询周期、只处理最近消息时间窗口、白名单用户 ID、评论提示词、AI Bot 是否允许表情、运行日志、AI 已发回复消息日志、已回复记录、小黑盒表情短码缓存、屏蔽关键词、等级过滤规则和相关本地偏好。所有设置仅保存在用户浏览器本地，不会上传到插件作者服务器。
```

## 单一用途说明

```text
本插件的单一用途是增强小黑盒网页社区的浏览与互动体验，包括信息流布局、评论预览、内容过滤，以及用户主动配置的 AI 总结和辅助评论。
```

## 数据使用声明

```text
不向插件开发者服务器收集或上传用户数据。
不出售用户数据。
不将用户数据用于广告或追踪。
AI 入口默认开启显示；用户可选择 OpenAI Compatible、OpenAI Responses、Anthropic 或 Gemini，并可拉取模型列表或手动填写模型。用户配置 AI 服务并点击总结后，当前帖子标题、正文、话题、图片链接和最多 30 条评论文本会由扩展后台发送到用户配置的 AI 接口用于生成总结；评论超过 30 条时优先选取点赞量更高的评论。用户关闭 AI 功能后，页面不再显示 AI 按钮。AI Bot 各项自动评论开关默认关闭，首次进入时要求用户阅读风险说明并单独确认授权；用户主动开启后，插件会按相应开关查询当前登录账号的消息或首页推荐帖，并将消息内容、帖子详情、帖子图片链接、相关昵称或用户 ID、触发评论、最多 30 条评论上下文和启用时同一用户最近的跨帖子历史对话发送到用户配置的 AI 接口，用于生成自动评论，并代表当前网页登录账号发送。自动评论连续发送失败 3 次后会自动停止 AI 回复和首页暖贴，并提醒用户检查账号状态。API Key 保存在扩展本地存储，并可在网页内插件设置面板中编辑。
```

## 上架前仍需准备

- 至少 1 张商店截图，推荐尺寸 `1280x800` 或 `640x400`。
- 商店宣传图标，当前仓库已提供 `assets/icons/icon128.png` 可作为基础图标使用。
- 隐私政策链接。如果使用 GitHub Pages 或仓库页面发布，可指向 `PRIVACY.md` 的公开地址。
- 开发者账号资料、邮箱和支付注册信息。
